{
  "name": "salmalm-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build --config vite.config.ts && vite build --config vite.app.config.ts",
    "build:agents": "vite build --config vite.config.ts",
    "build:app": "echo 'WARNING: app.js is managed by bundle_js.py. Vite output → dist-app/app.vite.js (dev only)' && vite build --config vite.app.config.ts",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
